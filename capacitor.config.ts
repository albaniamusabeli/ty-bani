import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bani.tybani',
  appName: 'ty-bani',
  webDir: 'dist/ty-bani',
  bundledWebRuntime: false
};

export default config;
